model:
  name: "CatBoost"
  iterations: 500
  depth: 6
  learning_rate: 0.05
  l2_leaf_reg: 3.0
  subsample: 0.8
  early_stopping_rounds: 20

training:
  learning_rate: 0.05
  batch_size: 64
  max_epochs: 1  # not used directly by CatBoost, kept for config consistency
  patience: 15
